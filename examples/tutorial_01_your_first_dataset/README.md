# Tutorial 1: Import your data

Turns a CSV of experimental measurements into a `hybrax.format`
`BioProcessCollection`, saves it, and checks the round trip.

Narrated version: `docs/source/tutorials/01_your_first_dataset.md`.

## Run

```bash
python run.py
```

Writes `out/data.json`.
