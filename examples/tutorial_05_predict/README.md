# Tutorial 5: Predict processes

Trains the Tutorial 4 model again, then demonstrates both ways to use it:
`forward` from the command line, and `model_load` + `model_predict` from
Python when you want the arrays.

Narrated version: `docs/source/tutorials/05_predict.md`.

## Run

```bash
python run.py
```

Writes `out/run_1_fit.png` and prints the learned vs. true growth rate.
